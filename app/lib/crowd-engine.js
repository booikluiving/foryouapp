"use strict";

// ⚠️ MOVED — crowd-engine now lives in crowd-system/.
// This stub exists for backward compatibility.
// Prefer require("../crowd-system") directly.

module.exports = require("../../crowd-system");
