# Generated SQLite Schema Map

Generated by `npm run docs:architecture` from `CREATE TABLE`, `ALTER TABLE ADD COLUMN` and `CREATE INDEX` statements. JSON links such as `character_ids_json` are conceptual relations, not enforced foreign keys.

Total tables found: 30

## Table Groups

| Group | Count | Tables |
| --- | ---: | --- |
| admin | 1 | `admin_trusted_devices` |
| algorithm | 15 | `algorithm_character_vote_skips`, `algorithm_character_votes`, `algorithm_characters`, `algorithm_crossing_thresholds`, `algorithm_environments`, `algorithm_labels`, `algorithm_path_edges`, `algorithm_path_node_blocks`, `algorithm_path_scenes`, `algorithm_path_thresholds`, `algorithm_paths`, `algorithm_performers`, `algorithm_scene_runs`, `algorithm_scenes`, `algorithm_situations` |
| catalog mirror | 2 | `catalog_file_sync_state`, `catalog_sync_kv` |
| chat/moderation | 2 | `chat_messages`, `moderation_actions` |
| local sync | 2 | `sync_peer_state`, `sync_tombstones` |
| polls | 2 | `poll_votes`, `polls` |
| session | 5 | `session_access_grants`, `session_engagement_scores`, `session_join_events`, `session_join_tokens`, `sessions` |
| settings | 1 | `settings` |

## Tables

### admin_trusted_devices

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `selector` | TEXT NOT NULL UNIQUE |
| `token_hash` | TEXT NOT NULL |
| `label` | TEXT |
| `created_at` | TEXT NOT NULL |
| `last_used_at` | TEXT |
| `last_ip` | TEXT |
| `expires_at` | TEXT NOT NULL |
| `revoked_at` | TEXT |

Indexes:

- `idx_admin_trusted_devices_selector` on `selector`
- `idx_admin_trusted_devices_expiry` on `expires_at`

### algorithm_character_vote_skips

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `run_id` | INTEGER NOT NULL |
| `scene_id` | INTEGER NOT NULL |
| `client_key` | TEXT NOT NULL |
| `skipped_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_character_vote_skips_run` on `run_id`

### algorithm_character_votes

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `run_id` | INTEGER NOT NULL |
| `scene_id` | INTEGER NOT NULL |
| `character_id` | INTEGER NOT NULL |
| `client_key` | TEXT NOT NULL |
| `voted_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_character_votes_run` on `run_id, character_id`

### algorithm_characters

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `name` | TEXT NOT NULL |
| `description` | TEXT |
| `prompt_text` | TEXT |
| `performer_id` | INTEGER |
| `label_scores_json` | TEXT NOT NULL DEFAULT '{}' |
| `is_active` | INTEGER NOT NULL DEFAULT 1 |
| `archived_at` | TEXT |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_characters_active` on `is_active, archived_at, id`

### algorithm_crossing_thresholds

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `scene_id` | INTEGER NOT NULL |
| `required_count` | INTEGER NOT NULL DEFAULT 1 |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_crossing_thresholds_scene` on `scene_id, id`

### algorithm_environments

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `name` | TEXT NOT NULL |
| `description` | TEXT |
| `prompt_text` | TEXT |
| `label_scores_json` | TEXT NOT NULL DEFAULT '{}' |
| `is_active` | INTEGER NOT NULL DEFAULT 1 |
| `archived_at` | TEXT |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_environments_active` on `is_active, archived_at, id`

### algorithm_labels

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `name` | TEXT NOT NULL |
| `sort_order` | INTEGER NOT NULL DEFAULT 0 |
| `is_active` | INTEGER NOT NULL DEFAULT 1 |
| `archived_at` | TEXT |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_labels_active` on `is_active, archived_at, sort_order, id`

### algorithm_path_edges

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `path_id` | INTEGER NOT NULL |
| `from_scene_id` | INTEGER NOT NULL |
| `to_scene_id` | INTEGER NOT NULL |
| `edge_type` | TEXT NOT NULL DEFAULT 'required' |
| `sort_order` | INTEGER NOT NULL DEFAULT 0 |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_path_edges_path` on `path_id, sort_order, id`
- `idx_algorithm_path_edges_to_scene` on `to_scene_id, path_id`

### algorithm_path_node_blocks

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `path_id` | INTEGER NOT NULL |
| `source_scene_id` | INTEGER NOT NULL |
| `include_crossing_paths` | INTEGER NOT NULL DEFAULT 0 |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_path_node_blocks_path` on `path_id, source_scene_id, id`
- `idx_algorithm_path_node_blocks_path` on `path_id, source_scene_id, id`

### algorithm_path_scenes

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `path_id` | INTEGER NOT NULL |
| `scene_id` | INTEGER NOT NULL |
| `sort_order` | INTEGER NOT NULL DEFAULT 0 |
| `is_end_node` | INTEGER NOT NULL DEFAULT 0 |
| `ignore_crossing_blocks` | INTEGER NOT NULL DEFAULT 0 |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_path_scenes_path` on `path_id, sort_order, id`
- `idx_algorithm_path_scenes_scene` on `scene_id, path_id`

### algorithm_path_thresholds

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `path_id` | INTEGER NOT NULL |
| `source_scene_id` | INTEGER NOT NULL |
| `required_count` | INTEGER NOT NULL DEFAULT 1 |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_path_thresholds_path` on `path_id, source_scene_id, id`
- `idx_algorithm_path_thresholds_source_scene` on `source_scene_id, path_id`

### algorithm_paths

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `name` | TEXT NOT NULL |
| `description` | TEXT |
| `sort_order` | INTEGER NOT NULL DEFAULT 0 |
| `color` | TEXT |
| `edge_mode` | TEXT NOT NULL DEFAULT 'legacy' |
| `is_active` | INTEGER NOT NULL DEFAULT 1 |
| `archived_at` | TEXT |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_paths_active` on `is_active, archived_at, sort_order, id`

### algorithm_performers

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `name` | TEXT NOT NULL |
| `sort_order` | INTEGER NOT NULL DEFAULT 0 |
| `role_slot` | INTEGER NOT NULL DEFAULT 0 |
| `is_active` | INTEGER NOT NULL DEFAULT 1 |
| `archived_at` | TEXT |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_performers_active` on `is_active, archived_at, sort_order, id`

### algorithm_scene_runs

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `scene_id` | INTEGER NOT NULL |
| `run_order` | INTEGER NOT NULL |
| `selection_source` | TEXT |
| `started_at` | TEXT NOT NULL |
| `ended_at` | TEXT |
| `heart_count` | INTEGER NOT NULL DEFAULT 0 |
| `bored_count` | INTEGER NOT NULL DEFAULT 0 |
| `comment_count` | INTEGER NOT NULL DEFAULT 0 |
| `score` | REAL |
| `prompt_snapshot` | TEXT |
| `reason` | TEXT |
| `random_seed` | TEXT |
| `resolved_character_slots_json` | TEXT NOT NULL DEFAULT '[]' |
| `resolved_environment_id` | INTEGER |
| `resolved_scene_json` | TEXT NOT NULL DEFAULT '{}' |
| `updated_at` | TEXT |

Indexes:

- `idx_algorithm_scene_runs_session` on `session_id, run_order, id`
- `idx_algorithm_scene_runs_open` on `session_id, ended_at, id`

### algorithm_scenes

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `title` | TEXT NOT NULL |
| `sort_order` | INTEGER NOT NULL DEFAULT 0 |
| `character_count` | INTEGER NOT NULL DEFAULT 1 |
| `character_slots_json` | TEXT NOT NULL DEFAULT '[]' |
| `character_ids_json` | TEXT NOT NULL DEFAULT '[]' |
| `situation_ids_json` | TEXT NOT NULL DEFAULT '[]' |
| `label_ids_json` | TEXT NOT NULL DEFAULT '[]' |
| `environment_id` | INTEGER |
| `environment_mode` | TEXT NOT NULL DEFAULT 'selected' |
| `context_scene_id` | INTEGER |
| `prompt_override` | TEXT |
| `is_active` | INTEGER NOT NULL DEFAULT 1 |
| `archived_at` | TEXT |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_scenes_active` on `is_active, archived_at, sort_order, id`

### algorithm_situations

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `name` | TEXT NOT NULL |
| `description` | TEXT |
| `prompt_text` | TEXT |
| `required_character_ids_json` | TEXT NOT NULL DEFAULT '[]' |
| `allowed_character_ids_json` | TEXT NOT NULL DEFAULT '[]' |
| `label_scores_json` | TEXT NOT NULL DEFAULT '{}' |
| `is_active` | INTEGER NOT NULL DEFAULT 1 |
| `archived_at` | TEXT |
| `created_at` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- `idx_algorithm_situations_active` on `is_active, archived_at, id`

### catalog_file_sync_state

Bron: `app/lib/dropbox-catalog-sync.js`

| Column | Type / constraints |
| --- | --- |
| `file_path` | TEXT PRIMARY KEY |
| `entity` | TEXT NOT NULL |
| `entity_id` | INTEGER NOT NULL DEFAULT 0 |
| `content_hash` | TEXT |
| `db_updated_at` | TEXT |
| `file_mtime_ms` | INTEGER NOT NULL DEFAULT 0 |
| `status` | TEXT NOT NULL DEFAULT 'synced' |
| `last_synced_at` | TEXT NOT NULL |
| `conflict_path` | TEXT |
| `last_error` | TEXT |

Indexes:

- `idx_catalog_file_sync_entity` on `entity, entity_id`

### catalog_sync_kv

Bron: `app/lib/dropbox-catalog-sync.js`

| Column | Type / constraints |
| --- | --- |
| `key` | TEXT PRIMARY KEY |
| `value` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- Geen expliciete index gevonden in de extractie.

### chat_messages

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `time` | TEXT NOT NULL |
| `client_id` | INTEGER |
| `client_key` | TEXT |
| `ip` | TEXT |
| `name` | TEXT |
| `text` | TEXT |
| `status` | TEXT NOT NULL |
| `detail` | TEXT |

Indexes:

- `idx_chat_messages_session` on `session_id, id`
- `idx_chat_messages_status` on `session_id, status`

### moderation_actions

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `time` | TEXT NOT NULL |
| `action_type` | TEXT NOT NULL |
| `client_key` | TEXT NOT NULL |
| `client_label` | TEXT |
| `reason` | TEXT |
| `expires_at` | TEXT |
| `created_by` | TEXT |

Indexes:

- `idx_moderation_actions_session` on `session_id, id`
- `idx_moderation_actions_client` on `session_id, client_key, id`

### poll_votes

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `poll_id` | INTEGER NOT NULL |
| `client_key` | TEXT NOT NULL |
| `option_index` | INTEGER NOT NULL |
| `voted_at` | TEXT NOT NULL |

Indexes:

- `idx_poll_votes_poll` on `poll_id, option_index`

### polls

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `question` | TEXT NOT NULL |
| `options_json` | TEXT NOT NULL |
| `status` | TEXT NOT NULL |
| `started_at` | TEXT NOT NULL |
| `ended_at` | TEXT |
| `created_by` | TEXT |
| `duration_seconds` | INTEGER NOT NULL DEFAULT ${DEFAULT_POLL_DURATION_SECONDS} |

Indexes:

- `idx_polls_session` on `session_id, id`
- `idx_polls_status` on `status, id`

### session_access_grants

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `grant_id` | TEXT NOT NULL UNIQUE |
| `token` | TEXT |
| `created_at` | TEXT NOT NULL |
| `expires_at` | TEXT NOT NULL |
| `last_seen_at` | TEXT |
| `last_ip` | TEXT |
| `user_agent` | TEXT |
| `source` | TEXT |

Indexes:

- `idx_session_access_grants_session` on `session_id, id`
- `idx_session_access_grants_expiry` on `expires_at`

### session_engagement_scores

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `session_id` | INTEGER NOT NULL |
| `engagement_key` | TEXT NOT NULL |
| `client_key` | TEXT NOT NULL |
| `client_tag` | TEXT |
| `name` | TEXT |
| `ip` | TEXT |
| `is_bot` | INTEGER NOT NULL DEFAULT 0 |
| `emoji_count` | INTEGER NOT NULL DEFAULT 0 |
| `comment_count` | INTEGER NOT NULL DEFAULT 0 |
| `emoji_points` | INTEGER NOT NULL DEFAULT 0 |
| `comment_points` | INTEGER NOT NULL DEFAULT 0 |
| `last_emoji_at` | TEXT |
| `last_comment_at` | TEXT |
| `last_activity_at` | TEXT |

Indexes:

- `idx_session_engagement_scores_session` on `session_id, last_activity_at`

### session_join_events

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `token` | TEXT NOT NULL |
| `joined_at` | TEXT NOT NULL |
| `ip` | TEXT |
| `user_agent` | TEXT |
| `source` | TEXT |

Indexes:

- `idx_session_join_events_session` on `session_id, id`

### session_join_tokens

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `session_id` | INTEGER NOT NULL |
| `token` | TEXT NOT NULL UNIQUE |
| `created_at` | TEXT NOT NULL |
| `expires_at` | TEXT NOT NULL |
| `use_count` | INTEGER NOT NULL DEFAULT 0 |
| `last_used_at` | TEXT |
| `created_by` | TEXT |

Indexes:

- `idx_session_join_tokens_session` on `session_id, id`
- `idx_session_join_tokens_expiry` on `expires_at`

### sessions

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `name` | TEXT NOT NULL |
| `started_at` | TEXT NOT NULL |
| `ended_at` | TEXT |
| `updated_at` | TEXT |

Indexes:

- Geen expliciete index gevonden in de extractie.

### settings

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `key` | TEXT PRIMARY KEY |
| `value` | TEXT NOT NULL |
| `updated_at` | TEXT NOT NULL |

Indexes:

- Geen expliciete index gevonden in de extractie.

### sync_peer_state

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `peer_url` | TEXT PRIMARY KEY |
| `last_ok_at` | TEXT |
| `last_error_at` | TEXT |
| `last_error` | TEXT |
| `last_seen_device_id` | TEXT |
| `last_seen_build` | TEXT |
| `last_received_change_at` | TEXT |
| `updated_at` | TEXT NOT NULL |

Indexes:

- Geen expliciete index gevonden in de extractie.

### sync_tombstones

Bron: `app/server.js`

| Column | Type / constraints |
| --- | --- |
| `entity` | TEXT NOT NULL |
| `entity_id` | INTEGER NOT NULL |
| `deleted_at` | TEXT NOT NULL |
| `updated_by_device_id` | TEXT |

Indexes:

- Geen expliciete index gevonden in de extractie.
